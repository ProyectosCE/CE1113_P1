# AuraBot hardware PWM support for the Raspberry Pi 4.
# Keep the SoC driver built in so the minimal image does not need a separate
# kernel module package or a module-loading policy.
CONFIG_PWM=y
CONFIG_PWM_SYSFS=y
CONFIG_PWM_BCM2835=y
